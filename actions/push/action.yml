name: Push to cupboard
description: Push Nix store paths to a cupboard cache from GitHub Actions.
inputs:
  url:
    description: cupboard Worker URL.
    required: true
  paths:
    description:
      Newline-delimited local Nix store paths, derivations, or installables to
      push. Required unless root-groups is given.
    required: false
  cupboard-path:
    description:
      Pre-acquired cupboard executable from actions/setup. Skips release
      installation.
    required: false
  cupboard-version:
    description:
      cupboard version to install. Use latest or an exact published release tag.
    required: false
  include-prereleases:
    description:
      When resolving latest, consider prereleases and install the newest release
      of any kind.
    required: false
  github-token:
    description: GitHub token used for release API calls.
    required: false
  release-repository:
    description: Repository that publishes cupboard release assets.
    required: false
  expected-source-commit:
    description:
      Full commit id the installed release must have been built from. Requires
      an exact cupboard-version.
    required: false
  install-dir:
    description: Directory for the downloaded cupboard binary.
    required: false
  cache:
    description: Named cache to push to.
    required: false
  store:
    description: >-
      Remote ssh-ng store URI the push reads path metadata and NAR bytes from.
      Empty reads the store Nix itself would use on this runner.
    required: false
  audience:
    description: GitHub OIDC audience. Defaults to url.
    required: false
  root:
    description: Retention root for pushed paths.
    required: false
  ttl:
    description: Retention TTL such as 7d or 12h.
    required: false
  no-retain:
    description:
      Publish without any retention root or pin; kept only by the cache's
      retention grace policy, if one matches. Conflicts with root and ttl.
    required: false
    default: 'false'
  wait:
    description: Wait for deferred blobs to become servable.
    required: false
    default: 'true'
  wait-timeout:
    description:
      Wait timeout, applied separately to commit capacity and to deferred blobs.
    required: false
    default: 10m
  attestations:
    description: Newline-delimited local Sigstore DSSE bundle paths to attach.
    required: false
  require-grace:
    description:
      Fail unless every pushed path reports a positive retention grace deadline.
      Requires wait to stay enabled.
    required: false
    default: 'false'
  intermediate-paths-file:
    description:
      Newline-delimited store paths to publish alongside the targets without
      retaining them as targets. Carried by only the first push when root-groups
      declares more than one root.
    required: false
  reference-paths-file:
    description:
      Newline-delimited store paths the tenant already holds, published from
      reference-source with no local store read or NAR upload. Required together
      with reference-source.
    required: false
  reference-source:
    description:
      Served cache endpoint the reference paths are read from. Required together
      with reference-paths-file.
    required: false
  run-root:
    description:
      Bind a run root, so every pushed path also joins this root as it commits;
      independent of root or root-groups.
    required: false
  run-root-ttl:
    description: Expire the run root after this duration. Requires run-root.
    required: false
  root-groups:
    description: >-
      JSON array of {root, paths} objects: one push per group, replacing the
      flat paths and root inputs. Use when a cohort declares more than one
      target root. Each root is replaced atomically.
    required: false
outputs:
  cupboard-path:
    description: Path to the installed cupboard binary.
    value: ${{ steps.push.outputs.cupboard-path }}
  cupboard-version:
    description: Inspected cupboard version.
    value: ${{ steps.push.outputs.cupboard-version }}
  uploaded-paths:
    description: Number of store paths uploaded to the cache.
    value: ${{ steps.push.outputs.uploaded-paths }}
  reused-blobs:
    description: Number of paths already present in the cache and reused.
    value: ${{ steps.push.outputs.reused-blobs }}
  skipped-paths:
    description: Number of paths that required no upload.
    value: ${{ steps.push.outputs.skipped-paths }}
  uploaded-bytes:
    description: Total number of bytes uploaded to the cache.
    value: ${{ steps.push.outputs.uploaded-bytes }}
runs:
  using: composite
  steps:
    - name: Resolve the pinned toolchain
      id: toolchain
      shell: bash
      # The workspace manifest's packageManager field is the single pnpm pin;
      # the release integrity suffix, when present, is not part of the version.
      run: |
        manifest="$GITHUB_ACTION_PATH/../../package.json"
        node_version_file="$GITHUB_ACTION_PATH/../../.node-version"
        version="$(sed -n 's/.*"packageManager": *"pnpm@\([^+"]*\).*/\1/p' "$manifest")"
        if [ -z "$version" ]; then
          echo "::error::package.json declares no pnpm packageManager pin"
          exit 1
        fi
        if [ ! -r "$node_version_file" ]; then
          echo "::error::.node-version is not readable"
          exit 1
        fi
        node_version="$(tr -d '\r\n' < "$node_version_file")"
        if [ -z "$node_version" ]; then
          echo "::error::.node-version declares no Node version"
          exit 1
        fi
        echo "pnpm-version=$version" | tee -a "$GITHUB_OUTPUT"
        echo "node-version=$node_version" | tee -a "$GITHUB_OUTPUT"

    - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
      with:
        version: ${{ steps.toolchain.outputs.pnpm-version }}
        # Point pnpm/action-setup's manifest lookup at this action's own
        # manifest, not the caller's checkout, whose pinned pnpm would
        # otherwise conflict with `version`. Parity between this pin and the
        # workspace manifest is enforced by scripts/action-tool-parity.test.ts.
        package_json_file: ${{ github.action_path }}/../../package.json

    - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
      with:
        node-version: ${{ steps.toolchain.outputs.node-version }}

    - name: Install action dependencies
      shell: bash
      working-directory: ${{ github.action_path }}/../..
      run: pnpm install --filter @cupboard/action... --prod --frozen-lockfile

    - name: Push store paths
      id: push
      shell: bash
      env:
        URL: ${{ inputs.url }}
        PATHS: ${{ inputs.paths }}
        CUPBOARD_PATH: ${{ inputs.cupboard-path }}
        CUPBOARD_VERSION: ${{ inputs.cupboard-version }}
        INCLUDE_PRERELEASES: ${{ inputs.include-prereleases }}
        GH_TOKEN: ${{ inputs.github-token || github.token }}
        RELEASE_REPOSITORY: ${{ inputs.release-repository }}
        EXPECTED_SOURCE_COMMIT: ${{ inputs.expected-source-commit }}
        INSTALL_DIR: ${{ inputs.install-dir }}
        CACHE: ${{ inputs.cache }}
        STORE: ${{ inputs.store }}
        AUDIENCE: ${{ inputs.audience }}
        ROOT: ${{ inputs.root }}
        TTL: ${{ inputs.ttl }}
        # The program's option is `--retain`; only the two valid literals
        # invert here, so any other value reaches the strict boolean parse
        # unchanged and is refused there.
        RETAIN:
          ${{ inputs.no-retain == 'true' && 'false' || inputs.no-retain ==
          'false' && 'true' || inputs.no-retain }}
        WAIT: ${{ inputs.wait }}
        WAIT_TIMEOUT: ${{ inputs.wait-timeout }}
        ATTESTATIONS: ${{ inputs.attestations }}
        REQUIRE_GRACE: ${{ inputs.require-grace }}
        INTERMEDIATE_PATHS_FILE: ${{ inputs.intermediate-paths-file }}
        REFERENCE_PATHS_FILE: ${{ inputs.reference-paths-file }}
        REFERENCE_SOURCE: ${{ inputs.reference-source }}
        RUN_ROOT: ${{ inputs.run-root }}
        RUN_ROOT_TTL: ${{ inputs.run-root-ttl }}
        ROOT_GROUPS: ${{ inputs.root-groups }}
      run: |
        args=(push
          --url "$URL"
          --paths "$PATHS"
          --cupboard-path "$CUPBOARD_PATH"
          --cupboard-version "$CUPBOARD_VERSION"
          --include-prereleases "$INCLUDE_PRERELEASES"
          --github-token "$GH_TOKEN"
          --release-repository "$RELEASE_REPOSITORY"
          --expected-source-commit "$EXPECTED_SOURCE_COMMIT"
          --install-dir "$INSTALL_DIR"
          --cache "$CACHE"
          --store "$STORE"
          --audience "$AUDIENCE"
          --root "$ROOT"
          --ttl "$TTL"
          --retain "$RETAIN"
          --require-grace "$REQUIRE_GRACE"
          --wait "$WAIT"
          --wait-timeout "$WAIT_TIMEOUT"
          --attestations "$ATTESTATIONS"
          --intermediate-paths-file "$INTERMEDIATE_PATHS_FILE"
          --reference-paths-file "$REFERENCE_PATHS_FILE"
          --reference-source "$REFERENCE_SOURCE"
          --run-root "$RUN_ROOT"
          --run-root-ttl "$RUN_ROOT_TTL"
          --root-groups "$ROOT_GROUPS")
        node --experimental-transform-types \
          --disable-warning=ExperimentalWarning \
          "$GITHUB_ACTION_PATH/../src/main.ts" "${args[@]}"
