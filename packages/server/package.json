{
	"name": "@cupboard/server",
	"version": "0.0.0",
	"dependencies": {
		"@aws-sdk/client-s3": "^3.1062.0",
		"@aws-sdk/s3-request-presigner": "^3.1062.0",
		"@cupboard/nix": "workspace:*",
		"@cupboard/protocol": "workspace:*",
		"@sigstore/bundle": "^5.0.0",
		"drizzle-orm": "^0.45.2",
		"hono": "^4.12.23",
		"http-status-codes": "^2.3.0",
		"jose": "^6.2.3",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.16.13",
		"@cloudflare/workers-types": "^4.20260605.1",
		"drizzle-kit": "^0.31.10",
		"typescript": "^6.0.3",
		"vitest": "^4.1.8",
		"wrangler": "^4.98.0"
	},
	"main": "src/worker.ts",
	"private": true,
	"scripts": {
		"build-info": "node ../../scripts/build-info.ts",
		"cf:typegen": "pnpm build-info && wrangler types --env-file .typegen.vars --strict-vars false --env-interface Env && wrangler types -c wrangler.tenant.toml --env-file .typegen.tenant.vars --strict-vars false --env-interface TenantEnv --include-runtime false worker-configuration.tenant.d.ts",
		"cf:typegen:check": "pnpm build-info && wrangler types --env-file .typegen.vars --strict-vars false --env-interface Env --check worker-configuration.d.ts && wrangler types -c wrangler.tenant.toml --env-file .typegen.tenant.vars --strict-vars false --env-interface TenantEnv --include-runtime false --check worker-configuration.tenant.d.ts",
		"check:types": "pnpm cf:typegen:check && tsc --noEmit",
		"deploy": "pnpm cf:typegen && pnpm deploy:migrations && wrangler deploy -c wrangler.tenant.toml && wrangler deploy",
		"deploy:migrations": "wrangler d1 migrations apply CUPBOARD_DB --remote",
		"dev": "pnpm cf:typegen && wrangler dev -c wrangler.toml -c wrangler.tenant.toml",
		"test": "pnpm build-info && vitest run --passWithNoTests"
	},
	"type": "module"
}
